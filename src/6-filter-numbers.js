export const filterNumbers = (array, largerThan) => {
  return array.filter(number => number <= largerThan)
}

/*
export const filterNumbers = (array, largerThan) => {
  return array.splice(3, 2)
}


export const filterNumbers = (array, largerThan) => {
  return array.filter(number => number <= largerThan)
}


*/


// org:

/*

export const filterNumbers = (array, largerThan) => {
  return array
}


*/